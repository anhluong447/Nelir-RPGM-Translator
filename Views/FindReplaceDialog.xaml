<Window x:Class="Nelir.Views.FindReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find &amp; Replace" Height="270" Width="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgSurface}"
        Foreground="{DynamicResource TextPrimary}">
    <Grid Margin="10">
        <TabControl x:Name="MainTabControl" SelectionChanged="MainTabControl_SelectionChanged" Background="Transparent" BorderBrush="{DynamicResource BorderDefault}">
            
            <!-- FIND TAB -->
            <TabItem Header="Tìm kiếm (Find)" Height="28">
                <Grid Margin="10,15,10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Tìm kiếm:" 
                               Style="{StaticResource TextLabel}" Margin="0,0,10,10" VerticalAlignment="Center"/>
                    <TextBox x:Name="FindTextBox_Find" Grid.Row="0" Grid.Column="1" 
                             Style="{StaticResource InputDefault}" Margin="0,0,0,10"/>

                    <CheckBox x:Name="MatchCaseCheckBox_Find" Grid.Row="1" Grid.Column="1" 
                              Content="Khớp chữ hoa/thường (Match case)" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,15"/>

                    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <Button Content="Tìm tiếp (Find Next)" Width="140" Height="32" 
                                Style="{StaticResource ButtonPrimary}" Click="FindNextButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- REPLACE TAB -->
            <TabItem Header="Thay thế (Replace)" Height="28">
                <Grid Margin="10,15,10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Tìm kiếm:" 
                               Style="{StaticResource TextLabel}" Margin="0,0,10,10" VerticalAlignment="Center"/>
                    <TextBox x:Name="FindTextBox_Replace" Grid.Row="0" Grid.Column="1" 
                             Style="{StaticResource InputDefault}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Thay thế bằng:" 
                               Style="{StaticResource TextLabel}" Margin="0,0,10,10" VerticalAlignment="Center"/>
                    <TextBox x:Name="ReplaceTextBox_Replace" Grid.Row="1" Grid.Column="1" 
                             Style="{StaticResource InputDefault}" Margin="0,0,0,10"/>

                    <CheckBox x:Name="MatchCaseCheckBox_Replace" Grid.Row="2" Grid.Column="1" 
                              Content="Khớp chữ hoa/thường (Match case)" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,15"/>

                    <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                                Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <Button Content="Tìm tiếp" Width="85" Height="32" 
                                Style="{StaticResource ButtonSecondary}" Margin="0,0,8,0" Click="FindNextButton_Click"/>
                        <Button Content="Thay thế" Width="85" Height="32" 
                                Style="{StaticResource ButtonPrimary}" Margin="0,0,8,0" Click="ReplaceButton_Click"/>
                        <Button Content="Thay thế tất cả" Width="110" Height="32" 
                                Style="{StaticResource ButtonSecondary}" Click="ReplaceAllButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
